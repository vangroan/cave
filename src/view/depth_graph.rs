use daggy::Dag;

pub struct DepthGraph {}

impl DepthGraph {
    pub fn new() -> Self {
        DepthGraph {}
    }
}

impl Default for DepthGraph {
    fn default() -> Self {
        DepthGraph {}
    }
}
